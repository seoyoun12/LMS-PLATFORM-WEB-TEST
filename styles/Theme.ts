import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#ff9a67',
      main: '#ff5600',
      dark: '#E92A00',
    },
    secondary: {
      light: '#93CCFF',
      main: '#2897FB',
      dark: '#0369E0',
    },
  },
});

export default theme;

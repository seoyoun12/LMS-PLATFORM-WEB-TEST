import * as React from 'react';
import Container from '@mui/material/Container';

export function ApplyTutor() {

  return (
    <Container
      component="div"
      maxWidth="xs"
      sx={{
        marginBottom: 8
      }}
    >
      APPLY TUTOR
    </Container>
  );
}
